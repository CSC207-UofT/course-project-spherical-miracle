Class name: ManageUser

Responsibilities:
* Check if new User.Entities.User is valid and then adds to HashMap in User.UserDatabase
* Remove User.Entities.User

Collaborators:
* User.UserDatabase
* User.Entities.User
